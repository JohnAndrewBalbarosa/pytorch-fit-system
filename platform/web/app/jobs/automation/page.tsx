import { ServiceWorkspace } from "@/components/service-workspace";
export default function Page() { return <ServiceWorkspace eyebrow="Job operations" title="Job Automation" description="Live goals, browser work, intervention queues, and safe recovery from the existing job-finder service." endpoint="/api/backend/job-finder/control-state" safety="Local developer access does not authorize CAPTCHA bypass, questionnaire answers, uploads, Continue, Review, or final Submit." />; }
